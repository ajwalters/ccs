.prepend-4.span-16
  %h1 Thanks! We'll get back with you as soon as we have reviewed all the submissions.

  %h2 Full Name
  %p= "#{@model.full_name||"n/a"}"

  %h2 Title
  %p= "#{@model.title||"n/a"}"

  %h2 Description
  %p= "#{@model.description||"n/a"}"

  %h2 Email
  %p= "#{@model.email||"n/a"}"

  %h2 Homepage
  %p= "#{@model.homepage||"n/a"}"

  %h2 Biography
  %p= "#{@model.biography||"n/a"}"
