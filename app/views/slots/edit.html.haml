%h1 Editing slot
- form_for(@slot) do |f|
  = f.error_messages
  %p
    = f.label :time
    %br/
    = f.datetime_select :time
  %p
    = f.submit 'Update'
= link_to 'Show', @slot
|
\#{link_to 'Back', slots_path}
